---
name: bun-yarn-react-workflow
description: Use this skill when standardizing React project tooling for Bun runtime and Yarn-compatible workflows.
---

# Bun + Yarn React Workflow

## Diretrizes
- Definir `packageManager` no `package.json` para Bun.
- Manter scripts compatíveis (`dev`, `build`, `lint`).
- Garantir lockfile consistente do gerenciador adotado.
- Evitar mistura de ferramentas sem necessidade operacional.
