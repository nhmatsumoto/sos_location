name: mg-location-simulation

services:
  frontend:
    build:
      context: ./frontend-react
      dockerfile: Dockerfile
    container_name: simulador-mg-location
    ports:
      - "8080:8000"
    restart: unless-stopped
    networks:
      - mg-location-network

networks:
  mg-location-network:
    driver: bridge
